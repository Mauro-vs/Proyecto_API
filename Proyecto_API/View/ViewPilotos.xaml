<UserControl x:Class="Proyecto_API.View.ViewPilotos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#121212" Foreground="White">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentRed" Color="#D50000"/>

        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="bg" Fill="{TemplateBinding Background}" Width="40" Height="40"/>
                            <TextBlock Text="⬅" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                       FontSize="18" Foreground="{TemplateBinding Foreground}" Margin="0,-2,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Fill" Value="{StaticResource AccentRed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PilotCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Width" Value="280"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#252525" Offset="0"/>
                        <GradientStop Color="#151515" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Style="{StaticResource BackButtonStyle}" Click="BtnVolver_Click"/>

            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="PILOTOS 2026" FontFamily="Segoe UI Black" FontSize="28" Foreground="White"/>
                <Rectangle Width="40" Height="4" Fill="{StaticResource AccentRed}" HorizontalAlignment="Left" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ListaPilotosControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource PilotCardStyle}" MouseLeftButtonUp="TarjetaPiloto_Click" Tag="{Binding Competitor.Id}">
                            <Grid>
                                <TextBlock Text="{Binding Competitor.Abbreviation}" FontFamily="Segoe UI Black" FontSize="100" Foreground="#333" Opacity="0.2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-10,-20" IsHitTestVisible="False"/>

                                <Rectangle Width="6" HorizontalAlignment="Left" Fill="{Binding TeamColor}" RadiusX="2" RadiusY="2" Margin="0,10"/>

                                <StackPanel Margin="25,20,10,20" VerticalAlignment="Center">
                                    <Border Background="#333" HorizontalAlignment="Left" CornerRadius="4" Padding="6,2" Margin="0,0,0,10">
                                        <TextBlock Text="{Binding Competitor.CountryCode}" Foreground="#AAA" FontSize="10" FontWeight="Bold"/>
                                    </Border>

                                    <TextBlock Text="{Binding Competitor.FirstName}" Foreground="#CCC" FontSize="14"/>
                                    <TextBlock Text="{Binding Competitor.LastName}" Foreground="White" FontFamily="Segoe UI Black" FontSize="26" LineHeight="26"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                        <Ellipse Width="8" Height="8" Fill="{Binding TeamColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding TeamName}" Foreground="#888" FontSize="11" FontStyle="Italic"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>