<UserControl x:Class="Proyecto_API.View.ViewPilotos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#121212" Foreground="White">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentRed" Color="#D50000"/>

        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="bg" Fill="{TemplateBinding Background}" Width="40" Height="40"/>
                            <TextBlock Text="⬅" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                       FontSize="18" Foreground="{TemplateBinding Foreground}" Margin="0,-2,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Fill" Value="{StaticResource AccentRed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NoPhotoCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Width" Value="280"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#252525" Offset="0"/>
                        <GradientStop Color="#151515" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.3" Color="Black"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5, 0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentRed}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Style="{StaticResource BackButtonStyle}" Click="BtnVolver_Click"/>

            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="PARRILLA 2026" FontFamily="Segoe UI Black" FontSize="28" Foreground="White"/>
                <Rectangle Width="40" Height="4" Fill="{StaticResource AccentRed}" HorizontalAlignment="Left" Margin="0,5,0,0"/>
            </StackPanel>

            <Border Grid.Column="2" Background="#202020" CornerRadius="20" Width="280" Height="40" BorderBrush="#333" BorderThickness="1">
                <Grid Margin="15,0">
                    <TextBlock Text="🔍  Buscar..." VerticalAlignment="Center" Foreground="#555" IsHitTestVisible="False"/>
                    <TextBox Background="Transparent" BorderThickness="0" Foreground="White" VerticalAlignment="Center" FontSize="14" CaretBrush="Red"/>
                </Grid>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center">

                <Border Style="{StaticResource NoPhotoCardStyle}">
                    <Grid ClipToBounds="True">
                        <TextBlock Text="MAR" 
                                   FontFamily="Segoe UI Black" 
                                   FontSize="150" 
                                   Foreground="#333333" 
                                   Opacity="0.3"
                                   HorizontalAlignment="Right" 
                                   VerticalAlignment="Bottom" 
                                   Margin="0,0,-20,-40"
                                   IsHitTestVisible="False"/>

                        <Rectangle Width="6" HorizontalAlignment="Left" Fill="#D50000" RadiusX="2" RadiusY="2" Margin="0,10"/>

                        <StackPanel Margin="25,20,10,20" VerticalAlignment="Center">

                            <Border Background="#333" HorizontalAlignment="Left" CornerRadius="4" Padding="6,2" Margin="0,0,0,10">
                                <TextBlock Text="ESP" Foreground="#AAA" FontSize="10" FontWeight="Bold"/>
                            </Border>

                            <TextBlock Text="Marc" Foreground="#CCC" FontSize="14"/>
                            <TextBlock Text="MARQUEZ" Foreground="White" FontFamily="Segoe UI Black" FontSize="28" LineHeight="28"/>

                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                <Ellipse Width="8" Height="8" Fill="#D50000" Margin="0,0,8,0"/>
                                <TextBlock Text="Ducati Lenovo Team" Foreground="#888" FontSize="11" FontStyle="Italic"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>


                <Border Style="{StaticResource NoPhotoCardStyle}">
                    <Grid ClipToBounds="True">
                        <TextBlock Text="BAG" FontFamily="Segoe UI Black" FontSize="150" Foreground="#333333" Opacity="0.3"
                                   HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-20,-40"/>

                        <Rectangle Width="6" HorizontalAlignment="Left" Fill="#D50000" RadiusX="2" RadiusY="2" Margin="0,10"/>

                        <StackPanel Margin="25,20,10,20" VerticalAlignment="Center">
                            <Border Background="#333" HorizontalAlignment="Left" CornerRadius="4" Padding="6,2" Margin="0,0,0,10">
                                <TextBlock Text="ITA" Foreground="#AAA" FontSize="10" FontWeight="Bold"/>
                            </Border>
                            <TextBlock Text="Pecco" Foreground="#CCC" FontSize="14"/>
                            <TextBlock Text="BAGNAIA" Foreground="White" FontFamily="Segoe UI Black" FontSize="28" LineHeight="28"/>
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                <Ellipse Width="8" Height="8" Fill="#D50000" Margin="0,0,8,0"/>
                                <TextBlock Text="Ducati Lenovo Team" Foreground="#888" FontSize="11" FontStyle="Italic"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource NoPhotoCardStyle}">
                    <Grid ClipToBounds="True">
                        <TextBlock Text="QUA" FontFamily="Segoe UI Black" FontSize="150" Foreground="#333333" Opacity="0.3"
                                   HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-20,-40"/>

                        <Rectangle Width="6" HorizontalAlignment="Left" Fill="#0055AA" RadiusX="2" RadiusY="2" Margin="0,10"/>

                        <StackPanel Margin="25,20,10,20" VerticalAlignment="Center">
                            <Border Background="#333" HorizontalAlignment="Left" CornerRadius="4" Padding="6,2" Margin="0,0,0,10">
                                <TextBlock Text="FRA" Foreground="#AAA" FontSize="10" FontWeight="Bold"/>
                            </Border>
                            <TextBlock Text="Fabio" Foreground="#CCC" FontSize="14"/>
                            <TextBlock Text="QUARTARARO" Foreground="White" FontFamily="Segoe UI Black" FontSize="24" LineHeight="24"/>
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                <Ellipse Width="8" Height="8" Fill="#0055AA" Margin="0,0,8,0"/>
                                <TextBlock Text="Monster Energy Yamaha" Foreground="#888" FontSize="11" FontStyle="Italic"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>