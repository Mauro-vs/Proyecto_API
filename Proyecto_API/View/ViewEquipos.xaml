<UserControl x:Class="Proyecto_API.View.ViewEquipos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#121212" Foreground="White">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentRed" Color="#D50000"/>

        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="bg" Fill="{TemplateBinding Background}" Width="40" Height="40"/>
                            <TextBlock Text="⬅" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" Foreground="White"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Fill" Value="{StaticResource AccentRed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TeamCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Width" Value="320"/>
            <Setter Property="Height" Value="240"/>
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.3" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource BackButtonStyle}" Click="BtnVolver_Click"/>
            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="EQUIPOS MOTOGP" FontFamily="Segoe UI Black" FontSize="28" Foreground="White"/>
                <Rectangle Width="40" Height="4" Fill="{StaticResource AccentRed}" HorizontalAlignment="Left" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ListaEquiposControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource TeamCardStyle}">
                            <Grid ClipToBounds="True">
                                <TextBlock Text="{Binding Team.Abbreviation}" FontFamily="Segoe UI Black" FontSize="100" 
                                           Foreground="#333" Opacity="0.1" HorizontalAlignment="Right" 
                                           VerticalAlignment="Bottom" Margin="0,0,-10,-20"/>

                                <Rectangle Width="6" HorizontalAlignment="Left" Fill="{Binding TeamColor}" RadiusX="2" RadiusY="2" Margin="0,10"/>

                                <StackPanel Margin="25,20,20,15">
                                    <TextBlock Text="{Binding Team.Name}" Foreground="White" FontFamily="Segoe UI Black" FontSize="22" TextWrapping="Wrap"/>
                                    <Rectangle Height="1" Fill="#333" Margin="0,10"/>

                                    <TextBlock Text="PILOTOS:" Foreground="#888" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>

                                    <ItemsControl ItemsSource="{Binding Competitors}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" Foreground="#CCC" FontSize="13" Margin="0,2"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <TextBlock Text="MotoGP Spec" Foreground="{Binding TeamColor}" FontSize="11" FontWeight="Bold" VerticalAlignment="Bottom" Margin="0,20,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>